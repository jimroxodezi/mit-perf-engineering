import time, random


n = 1024

A = [[random.random() for row in range(n)] for column in range(n)]
B = [[random.random() for row in range(n)] for column in range(n)]

C = [[0 for row in range(n)] for colunm in range(n)]


